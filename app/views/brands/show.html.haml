= render partial: "side_nav"
.container#brands
  .sub-nav-section.text-xs-center
    %nav.nav.nav-inline
      - @our_brands.each do |brand|
        = link_to brand.name, brand_path(brand.id), class: "nav-item sub-links"
- if params[:id] === "1"
  = render partial: "xaxis_materials", brand: @brand
