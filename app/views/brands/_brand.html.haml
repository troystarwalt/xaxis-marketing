.col-md-7.offset-md-1.col-sm-12
  .brand-assets-container
    .section#assets-welcome
      = image_tag "brand-logos/#{@brand.slug}-logo.png", class: "img-fluid brand-assets-top-logo"
      %p Welcome to the #{@brand.name} Brand Identity Guidelines. At #{@brand.name}, we are proud of our brand identity and the numerous elements that comprise the #{@brand.name} brand. From our letterhead to our website, #{@brand.name} represents powerful innovation, rich insights and a vision that keeps us at the cutting edge of the industry. By embracing the #{@brand.name} logo and collateral, you are taking part in our mission to define the digital realm and bring only the greatest success to our clients, partners and teams.
      %p The #{@brand.name} logo and visual identity system help bring this promise to life. These guidelines will ensure that we successfully implement the #{@brand.name} identity system consistently across all of our communications. This is especially important to establish presence and prominence with our key audiences.
      %p We hope this identity system inspires you to create brilliant materials for the #{@brand.name} brand.
      - if @brand.brand_accessories.has_category("guidelines").present?
        = link_to "download brand guidelines", @brand.brand_accessories.has_category("guidelines").last.file_url, class: "brand-btn-primary #{@brand.slug}-btn-primary"
    .section#assets-download-section
      %h1 #{@brand.name} Brand Assets
      %p We have a few guidelines for using our brand resources—please take a moment to familiarize yourself with them. You can download individual assets in each section. If you want everything, use the button below to download a .zip file:
      - if @group.present?
        = link_to "download all assets", brand_path(format: :zip), class: "brand-btn-secondary #{@brand.slug}-btn-secondary"
    .section#assets-logo
      %h3.text-uppercase #{@brand.name} Logos
      %p Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.
      = image_tag "brand-logos/#{@brand.slug}-logo.png", class: "img-fluid brand-assets-logo"
      - if @brand.brand_accessories.has_category("logo").present?
        = link_to "download logos", @brand.brand_accessories.has_category("logo").last.file_url, class: "brand-btn-primary #{@brand.slug}-btn-primary"
    %hr
    .section#assets-color-palette
      = render partial: "brands/palettes/#{@brand.slug}-p"
    %hr
    .section#assets-fonts
      %h3.text-uppercase #{@brand.name} fonts
      .row{class: "#{@brand.slug}-top" }
        .col-sm-3
          %p.font-name= get_font_name(@brand.slug, "a")
          %p.font-italic.font-use For Powerpoint and Word Use
        .col-sm-9
          %p.list-of-chars= raw get_chars
      %br
      .row{class: "#{@brand.slug}-bottom" }
        .col-sm-3
          %p.font-name= get_font_name(@brand.slug, "b")
          %p.font-italic.font-use For web and print use
        .col-sm-9
          %p.list-of-chars= raw get_chars
      - if @brand.brand_accessories.has_category("font").present?
        = link_to "download fonts", @brand.brand_accessories.has_category("font").last.file_url, class: "brand-btn-primary #{@brand.slug}-btn-primary"
    %hr
    .section#assets-images
      %h3.text-uppercase images
      %p Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu
      - if @image_bank.present?
        = link_to "download image bank", @image_bank, class: "brand-btn-primary #{@brand.slug}-btn-primary"
      - else
        %p No image bank available for download at this time.
    %hr
    .section#assets-ppt
      %h3.text-uppercase #{@brand.name} powerpoint template
      =image_tag "brand-template-image/#{@brand.slug}-tp.png", class: "img-fluid"
      - if @brand.brand_accessories.has_category("ppt_template").present?
        = link_to "download template", @brand.brand_accessories.has_category("ppt_template").last.file_url, class: "brand-btn-primary #{@brand.slug}-btn-primary"
    %hr
    .section#assets-word
      %h3.text-uppercase word template
      - if @brand.brand_accessories.has_category("word_template").present?
        = link_to "download template", @brand.brand_accessories.has_category("word_template").last.file_url, class: "brand-btn-primary #{@brand.slug}-btn-primary"
    %hr
    .section#assets-facts
      %h3.text-uppercase #{@brand.name} fast facts
      - if @brand.brand_accessories.has_category("facts").present?
        = link_to "download pdf", @brand.brand_accessories.has_category("facts").last.file_url, class: "brand-btn-primary #{@brand.slug}-btn-primary"
    %hr
    .section#assets-capability
      %h3.text-uppercase global capability deck
      - if @brand.brand_accessories.has_category("capability").present?
        = link_to "download deck", @brand.brand_accessories.has_category("capability").last.file_url, class: "brand-btn-primary #{@brand.slug}-btn-primary"
    %hr
    .section#assets-headshots
      %h3.text-uppercase executive headshots
      - unless @headshots.blank?
        = render partial: "headshots", brand: @brand
      - if @headshots.blank?
        %h4 No headshots available at this time.
